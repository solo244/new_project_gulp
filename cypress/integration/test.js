//var pkg = require("/package.json");
//var $url = pkg.url.local;

describe('Server up', function() {
  it('See if your server is running', function() {
    cy.visit('/')
    //cy.contains('type')
  })
})